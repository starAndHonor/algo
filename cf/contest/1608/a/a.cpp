#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
int main(int argc, char const *argv[]) {
  int T;
  cin >> T;
  int n;
  while (T--) {
    cin >> n;
    if (n == 1) {
      cout << 1 << endl;
      continue;
    }
    for (size_t i = 0; i < n; i++) {
      cout << 2 + n < " ";
    }
    puts("");
  }

  return 0;
}
